# Рантайм-зависимости
pystray>=0.19.5
Pillow>=10.4.0
pywebview>=5.1

# Только для сборки в .exe (шаг "Сборка" в README), в рантайме не нужен
pyinstaller>=6.6.0

# pywin32 сюда не входит: несмотря на то, что было в списке раньше,
# нигде в коде он не импортируется (import win32*) — весь Win32-доступ
# (трей, чтение активного окна, реестр для автозапуска, панель эмодзи,
# звук уведомлений) идёт либо через ctypes напрямую, либо через
# стандартные модули Python (winreg, winsound). Если в будущем
# добавится реальная зависимость от pywin32 — верните строку обратно.
